<div class="row">
    {{#each notes}}
    <div class="col-md-3">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title d-flex justify-content-between align-items-center">
                    {{eps}} 
                </h4>
                <hr>
                <h4 class="card-title d-flex justify-content-between align-items-center">
                    {{description}}
                <form action="/medicaments/{{nit}}" method="GET">
                        <button class="btn btn-success btn-block btn-sm" type="submit">View Medicaments</button>
                    {{!-- <a href="/notes/edit/{{_id}}" class="fas fa-edit">View Medicaments</a> --}}

                </form>
            </div>
        </div>
        <br>
    </div>
    {{/each}}
</div>